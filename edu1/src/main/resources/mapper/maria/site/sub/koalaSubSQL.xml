<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="koalaSubDAO">
	
	
<!-- 회원가입 -->
	<insert id="Singup">
		insert into USER
			( ID, PW, NICK ,BIRTH ,PREFER  )
		values
			( #{ID}, #{PW} , #{NICK} ,#{BIRTH} ,#{PREFER} )
	</insert>
	
</mapper>
